services:
   app:
      image: reversi-dev
      build:
         context: ../
         dockerfile: docker/Dockerfile.dev
      volumes:
         - ../:/usr/src/app
      container_name: reversi-dev
      ports:
         - 3005:3000
      env_file:
         - ../.env
