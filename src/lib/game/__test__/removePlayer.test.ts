import { removePlayer } from '../removePlayer';

describe('removePlayer', () => {
   test('reminder to add tests', () => {
      expect(() => removePlayer('testGame', 'testPlayer')).toThrow(
         'not currently supported.'
      );
   });
});
